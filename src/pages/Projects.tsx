import React from "react";

export default function Projects(): JSX.Element {
  return (
    <section id="projects" className="min-h-[70vh] flex items-center justify-center px-6">
      <div className="max-w-5xl">
        <h2 className="text-4xl font-bold mb-4">Projects</h2>
        <p className="text-cream/80">Add your project cards here — use GSAP to animate card entrances and hovers.</p>
      </div>
    </section>
  );
}
